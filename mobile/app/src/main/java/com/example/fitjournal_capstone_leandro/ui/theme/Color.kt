package com.example.fitjournal_capstone_leandro.ui.theme

import androidx.compose.ui.graphics.Color

val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)

// ---------------------------------------------------------------------------
// FitJournal palette — the app's real colours, defined once and reused
// everywhere (screens, shared components). Referenced by name (e.g. AccentYellow)
// rather than through MaterialTheme.colorScheme, matching the app's style.
// ---------------------------------------------------------------------------

// Brand
val AccentYellow = Color(0xFFFFEB3B)   // primary accent (buttons, highlights)
val AccentBlue = Color(0xFF5595CE)     // occasional secondary accent

// Surfaces / backgrounds
val BackgroundDark = Color(0xFF1B1B1E) // app background
val SurfaceDark = Color(0xFF2C2C2E)    // cards / elevated surfaces
val CellDark = Color(0xFF242424)       // table cells, subtle fills

// Text
val TextWhite = Color(0xFFFFFFFF)
val TextGray = Color(0xFF8E8E93)       // secondary / muted text
val TextMutedBorder = Color(0xFF444444)// field borders, hairlines

// Feedback
val ErrorRed = Color(0xFFFF453A)

// Sticky-note palette (dashboard notes — black ink on yellow paper)
val NoteYellow = Color(0xFFF4D93E)
val NoteText = Color(0xFF1A1A1A)
val NoteTextSoft = Color(0xFF3A3320)
val NoteTape = Color(0x4DFFFFFF)