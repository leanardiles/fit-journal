# FitJournal Roadmap

FitJournal is a personal fitness tracking application. This roadmap tracks planned features, technical debt, and the path from local development to AWS production deployment.

**Last updated:** 12 September 2026

---

## Priority & Effort Legend

| Priority | Meaning |
|---|---|
| 🔴 P0 | Critical — security, breaking bugs, blockers |
| 🟠 P1 | High — significant feature or blocker to deployment |
| 🟡 P2 | Medium — quality improvement, not urgent |
| 🟢 P3 | Low — someday, nice to have |

| Effort | Estimate |
|---|---|
| XS | Under 1 hour |
| S  | Half a day |
| M  | 1–2 days |
| L  | 3–5 days |
| XL | Week or more |

| Scope | Meaning |
|---|---|
| Mobile | Android app only |
| Web | Web frontend only |
| Backend | API / DB / infra only |
| Mobile / Web | Both clients |
| All | Backend + both clients |

---

## 🎯 Up Next

The short list — what's most likely to be worked on in the next session or two.

| Item | Scope | Priority | Effort | Notes |
|---|---|---|---|---|
| Exercise grouping (supersets) on mobile | Mobile | 🟡 P2 | M | The last routine-editor feature gap vs web: manual days on web can group exercises into ordered supersets/blocks (A1, A2, B1…) that Log WOD renders in order. Mobile's manual days are a flat list — no grouping. Add the same opt-in grouping (group_index + position, already persisted by the backend and returned by get_routine) to the mobile routine editor's manual picker, and render grouped manual days in order in the mobile WOD/log screens. Most complex remaining parity piece and fully optional for launch (the app works without it). |
| Play Store launch prep | Mobile | 🟠 P1 | L | The non-code requirements to publish on Google Play. (1) Privacy policy — a hosted, publicly-accessible URL stating what data is collected and how it's used (the app stores email + fitness data); required by the Data Safety form. (2) Data Safety form — declare data collection/sharing/security in the Play Console. (3) $25 one-time Google Play developer account. (4) Closed testing requirement — new personal developer accounts must run a closed test with ~12 testers for ~14 days before production access. (5) Target SDK / app signing / store listing assets (icon, screenshots, description). Account-deletion flow is now in place (see Done), so this is unblocked. The deletion URL for the Data Safety form is https://app.fit-journal.com/web/delete-account. |

---

## 📋 Backlog

Lower priority, but tracked so they're not forgotten.

### Functional

| Item | Scope | Priority | Effort | Notes |
|---|---|---|---|---|
| Confirm before auto-generating a WOD when no exercises are selected | Web | 🟢 P3 | S | On the web Log WOD page, if a day has no manually-selected exercises, generation happens without an explicit heads-up — the user doesn't clearly choose "auto-pick for me." Mobile now confirms this (prompts before auto-generating on an empty day); web should match so the two clients behave the same. Add a short confirm ("No exercises selected for this day — generate one automatically?") before triggering automatic selection when the current day's selection is empty. If selections already exist, use them directly (no prompt), as both clients do. Small, self-contained change to logwod.html. |
| Separate logged weight from current weight | Backend / Web / Mobile | 🟡 P2 | M | Today a single value does double duty: `exercises.exercise_user_current_weight` is both the "current" weight pre-filled into the logger AND what gets written to `workout_logs.weight_used` on completion — editing the weight field always overwrites current, and the session logs whatever current is. This conflates two different ideas: (a) the weight I actually used in THIS session (history), and (b) the weight to pre-fill NEXT time. They should be able to differ — e.g. current is 50kg but today I only managed 45kg, so the session should log 45 while current stays 50 (or updates only if I choose). Rework so each logged set/session records its own `weight_used` independent of the exercise's carried-forward current weight, with a clear rule (or user choice) for when/whether logging updates current. Exact UX TBD — options include: log-only-by-default with an explicit "update my working weight?" prompt, or always log the session value and let current be edited separately. Touches: the workout logger (web getwod/logwod + mobile WorkoutScreen), possibly the completion endpoint's handling of weight, and the calendar/dashboard which already read per-session `weight_used`. Data model already supports it (`workout_logs.weight_used` is per-row); the change is behavioral, not schema. |
| Day-scoped next-workout selections | All | 🟡 P2 | M | `next_workout_selections` has no day attached — it's one global per-user set of selected exercises, so selecting exercises for one routine day overwrites any prior day's selection. Every current-day-anchored surface (web Log WOD, mobile Generate Workout) then has to *filter* the global selection down to the current day's exercises to avoid using another day's picks (mobile does this now; it's how the wrong-day bug was worked around). Proper fix: tie a selection to a day (add `routine_day_number` to the table, or an equivalent), so each day remembers its own curated selection independently and reads filter by day. Touches the backend table + the selection write/read on both clients (calendar select, toggle endpoint, generation). Behavioral + a small schema add. Until then, the client-side current-day filter is the stopgap. |
| Persist in-progress mobile workout across app restarts | Mobile | 🟢 P3 | M | The mobile WOD now persists across in-app navigation (Option C — the generated/in-progress workout survives moving between tabs, cleared only by explicit Discard or Complete), but it lives in the ViewModel (in-memory), so killing/backgrounding-out the app loses a mid-workout session. Persist the in-progress session (generated exercises + entered sets/reps/weight + the chosen day) to local storage (Room, already in use) and restore on launch, so a genuinely-in-progress workout survives an app kill. Deferred from the Option-C work; only matters for the app-restart edge, not day-to-day use. |
| User-selectable date format (US vs rest-of-world) | Mobile / Web | 🟢 P3 | S | Let the user choose their display date format — `MM-DD-YYYY` (US) vs `DD-MM-YYYY` (rest of world) — as a profile preference, mirroring the existing weight-unit (kg/lb) preference. Display-only: the wire/storage format stays ISO `YYYY-MM-DD` everywhere; only rendering changes. Mobile currently hardcodes `DD-MM-YYYY` in the manual-log date field (its default). Add a profile field + a setting toggle, and have date-display helpers read it. Pairs naturally with the unit preference in Profile Settings. || Secondary muscle groups on exercises | Web | 🟡 P3 | M | Add an optional secondary muscle group (or groups) to each exercise, alongside the existing primary. Today an exercise credits only its primary muscle, so compound lifts under-count assisting muscles (a Row counts as Back only, not Back + Biceps). With secondaries, the dashboard's sets-per-muscle summary can attribute partial or full volume to assisting muscles for a truer weekly picture. Touches: the exercise model/schema (new nullable field), the exercise create/edit UI (web + mobile), and the dashboard aggregation (decide whether a secondary counts as a full set or a fraction). Scope: Backend + Web + Mobile. |
| Graceful session-expiry / 401 handling | Mobile | 🟡 P2 | S | When a request returns 401 (expired/invalid token), the app currently shows a raw "HTTP 401" in red and leaves the user stuck (e.g. can't load the muscle dropdown / exercises). Instead: detect 401 globally (in `AuthInterceptor` or a response handler), clear the stored token, show a friendly "Session expired — please log in again" message, and redirect to the login screen. Surfaced when switching the app from the local to the production backend — a stale local-signed token was rejected by production (different SECRET_KEY) until a fresh login. Real users' 30-day tokens will eventually expire and hit this. Play Store readiness item. |
| Routine page breaks for users with no profile/routine set up | Web | 🟡 P2 | S | A newly-registered user who navigates directly to Routine (before setting up their profile/routine) hits a broken page. Likely cause: the page or its API assumes data that doesn't exist yet for a fresh user — most probably a missing `workout_state` row or empty `routine_days`/`routine_muscles_per_day`, causing a null/missing-row error (check the uvicorn traceback to confirm). Fix should handle the empty state gracefully: redirect to setup, or show an empty-state prompt, rather than erroring. Reproduce: register fresh user → go straight to Routine. |
| Harden `/next-workout/toggle` payload parsing | Backend | 🟢 P3 | XS | The toggle endpoint reads `data['user_id']`, `data['exercise_id']`, `data['is_selected']` via direct dict access, so a missing key throws a `KeyError` → 500 crash instead of a clean 400. Replace with validated access (a Pydantic schema, or `.get()` with explicit checks returning 422/400). Robustness, not security. Same endpoint could also adopt a proper request schema like the other endpoints (it currently takes a raw `dict`). **UPDATE (Jul 2026):** added `int()` coercion on `user_id`/`exercise_id` — fixed a real bug where the web Calendar sent a string `user_id` (from localStorage) and the strict int-vs-string ownership compare wrongly returned 403. The KeyError-on-missing-key hardening + adopting a Pydantic request schema still remain. |
| Grouping edit mode (reorder / move between groups) | Web | 🟢 P3 | M | Manual-day grouping is currently create-or-clear only: once saved you can rebuild from scratch (“Clear grouping”) but can't reorder exercises within a group, reorder groups, or move an exercise between groups without clearing. Add proper editing — the clean model is “each exercise has a group + a position, both directly editable, labels re-derived so invalid states are impossible” rather than arrow buttons. Manual-day only. |
| Reorder routine days (first-class) | Web | 🟢 P3 | M | Let the user reorder whole days (make Day 1 the new Day 3), not only add/delete at the end. Day numbers are positional (they drive the rotation cursor and past logs reference `routine_day_number`), so reordering = renumbering and must handle the cursor + historical-log attribution carefully. Deferred; add/delete-with-renumber already covers the common cases. |
| n8n email automation | Backend | 🟡 P2 | M | Send welcome/notification emails (e.g. on registration) via an n8n workflow. New skill. NOTE: requires Lambda to make an outbound call, which the current VPC setup (no NAT Gateway) blocks. Solve by either having n8n call *in* to the API, using SES via a VPC endpoint, or adding a NAT Gateway (~$32/mo — avoid). Needs a webhook + a real email service (SES/SendGrid) with deliverability setup. n8n itself would run outside the VPC (n8n Cloud or a small separate host), so it can send/receive mail freely; the blocker is only when *Lambda* needs to trigger the outbound call. |
| Self-service password reset (real flow) | All | 🟢 P3 | L | Replace the placeholder forgot-password page with a real reset: request-reset page → emailed time-limited token → set-new-password page → token validation + hash update. Needs email sending (blocked by the same VPC/outbound issue as n8n above), a reset-token table, two endpoints, and security hardening (random single-use tokens, no email-existence leak, rate limiting). Only worth it once the app has real users. |
| Reevaluate calendar display when routine changes | Mobile / Web | 🟡 P2 | L | Historical workouts hidden when routine restructured; consider per-session routine snapshot. Affects both clients' calendar views. |
| Support user-defined / dynamic muscle groups | All | 🟢 P3 | L | Replace `MuscleGroupEnum` with a `muscle_groups` table; no more schema migrations to add groups. Schema change + muscle dropdown UI in both clients. |
| "Always select" exercise flag in Calendar | All | 🟢 P3 | S | Pin/sticky-select an exercise so it's auto-selected every workout. Backend flag + UI surfacing in both calendars. |
| Calendar "All Days" — per-day WOD column strips | Web | 🟢 P3 | M | Web Calendar only — the mobile Calendar already does this correctly (last 10 sessions for the selected day). The web Calendar's "All Days" view currently shares one set of 10 WOD column dates across all days, so empty columns appear for days that weren't trained on those dates. Better: each day section shows the last 10 sessions for that specific day. Likely renders as multiple stacked tables under each Day header rather than one wide table. |
| Enable auto-deploy on push to main | Backend | 🟢 P3 | XS | Uncomment the `push:` block in `deploy.yml` so pushes to `main` auto-deploy. Do *after* the backend test gate exists, so broken code can't auto-ship. |
| Rename package / applicationId to com.leandroardiles.fitjournal | Mobile | 🟠 P1 | M | Current package + applicationId is `com.example.fitjournal_capstone_leandro` — drops the academic "capstone" framing and the discouraged `com.example.` placeholder (Google Play may reject `com.example.*`). New name decided: **`com.leandroardiles.fitjournal`** (developer namespace — collision-proof, ownership-honest; the competing "Fit Journal" app on Play uses its own namespace `kz.maestrosultan.fitjournal`, so no conflict; display name stays "FitJournal"). ⚠️ applicationId CANNOT change once published to Play, so MUST be done before launch. **Lessons from the first attempt (reverted via `git reset --hard` — do this in the MOBILE repo, not the root backend repo):** the rename changes TWO package segments at once (`example`→`leandroardiles` AND `fitjournal_capstone_leandro`→`fitjournal`), which confuses Android Studio's rename and produces broken half-states. Do it as TWO separate single-segment renames: (1) `fitjournal_capstone_leandro`→`fitjournal`, build, (2) `example`→`leandroardiles`, build. Turn OFF "Compact Middle Packages" in Project view first so segments are individually selectable. When the "package is present in multiple directories" dialog appears, rename ALL THREE (main, androidTest, unitTest) for consistency. **Execution checklist:** commit a clean mobile-repo state first (rollback point); use Refactor → Rename (never hand-edit `package` lines); build + run tests after EACH segment rename; then set `applicationId` + `namespace` in build.gradle.kts; check `google-services.json`/Firebase (firebase-perf dependency is package-bound); rebuild + full test suite + smoke-test. Isolated change — nothing else in flight. The `R`-import errors seen mid-attempt are symptoms of inconsistent package state, not the real problem — they resolve once packages are consistent. |
| Make Calendar unit indicator interactive | Mobile | 🟢 P3 | S | Currently shows "Showing weights in KG" as a non-interactive label below the action buttons. Consider making it tappable to either (a) deep-link to ProfileSettingsScreen with the unit field highlighted, or (b) offer an inline kg/lb toggle that calls the profile-update endpoint and refreshes the calendar. Option (b) is better UX but more work. Wait until at least one user complains about wanting to switch easily — until then, profile settings is a reasonable place for this rarely-changed preference. |
| Calendar polish — empty states, swipe affordance, date format | Mobile | 🟡 P2 | S | Finish off the polish on the mobile Calendar. (1) **Empty states**: handle "no routine yet" (`daysPerWeek == 0`), "day has no exercises in library for its muscle groups", "no logged sessions yet for the selected day" — currently these render blank or near-blank, which is honest but unhelpful. (2) **Right-edge fade + "← swipe →" hint** under the table to signal there are more session columns to scroll. (3) **Date format upgrade**: column headers currently show `04-20` (YYYY-MM-DD truncated to MM-DD); upgrade to `20 Apr` with locale handling. (4) Possibly remove the tiny "current · viewing" legend once the dual-marker convention is familiar. None are blockers — feature is fully usable as-is. |

### UI / UX

| Item | Scope | Priority | Effort | Notes |
|---|---|---|---|---|
| Configurable dashboard modules (choose which to show) | Mobile / Web | 🟢 P3 | L | Let the user pick which dashboard sticky notes they see — some want the current routine, others want stats or sets-per-muscle — rather than a fixed set. Idea: a fixed cap of ~3 visible modules chosen from a pre-set catalog (quick stats, current routine, sets per muscle, and future ones), with a `+` affordance at the bottom of the dashboard to add a module and a way to hide/remove one. Needs: a catalog of available modules, per-user storage of the chosen set/order, and a render loop over the chosen list (the sticky-note pattern already makes each module self-contained, so rendering a dynamic list is straightforward). The real work is the **per-user module config + persistence** (a profile/settings field or a small table), shared with the draggable-reorder item below — build the two together so persistence isn't done twice. Both clients. |
| Draggable dashboard sticky notes (reorder) | Mobile / Web | 🟢 P3 | M | Let the user drag the dashboard sticky notes to reorder them (e.g. move sets-per-muscle above the routine). Mechanism exists on mobile (the `sh.calvin.reorderable` library already used in the WOD list) and is straightforward on web (HTML5 drag or a small lib), but reorder only makes sense if the order **persists per user** — otherwise it resets every load. That persistence is the same per-user dashboard-config storage the configurable-modules item needs, so do these two together. Minor visual note: drag handles on tilted sticky notes need care so the affordance reads clearly. Both clients. |
| Re-theme mobile app to match web's dark notebook aesthetic | Mobile | 🟡 P2 | L | Currently the mobile app uses the default light Material theme; the web app uses a dark notebook look (`--bg: #171717`, ruled lines, red margin, Patrick Hand font). Unify mobile with it. Scope to decide: (A) minimal — dark background + a proper Material 3 dark ColorScheme + fix text/contrast across all screens; or (B) full — replicate the notebook visuals (lines, margin, font, components). Touches every screen + theme files (`Theme.kt`, `Color.kt`); requires auditing hardcoded colors and checking contrast/readability per screen. Do as a dedicated effort, screen by screen — a half-applied dark theme leaves unreadable text. The splash screen already uses `#171717`, so the launch→app transition is a motivator for consistency. |
| Create a landing page for fit-journal.com | Web | 🟡 P2 | M | Marketing page at the bare domain. Hero, features, screenshots, link to "Sign up" → app.fit-journal.com/web/register. Notebook aesthetic consistent with the app. Likely a single static HTML page hosted on S3 + CloudFront (cheap, fast, HTTPS-ready via ACM). Separate from the FastAPI app. Trigger: when about to put fit-journal.com on CV. |
| Re-evaluate modal visibility issue | Web | 🟡 P2 | S | Deferred earlier; noted in code TODO. |
| Audit hardcoded greys (`#ccc`, `#aaa`, `#e0e0e0`) | Web | 🟢 P3 | XS | Replace with `var(--text)` / `var(--muted)` for proper light-mode adaptation. |
| Sweep templates for bare-path page links | Web | 🟢 P3 | XS | Page links must use the `/web/` prefix (e.g. `/web/routine`, `/web/dashboard`), not bare `/routine` or `/dashboard` — bare paths 404 (the route is `/web/...`; bare `/routine/{id}` etc. are API endpoints, leave those alone). Several of these have been fixed ad hoc (dashboard routine links, getwod back-to-dashboard). A `grep -rn 'href="/' templates/` sweep would catch any remaining ones. |

### Deployment & Security

| Item | Scope | Priority | Effort | Notes |
|---|---|---|---|---|
| Use a separate production SECRET_KEY | Backend | 🟢 P3 | XS | Local `.env` and the Lambda prod env currently share the same (strong, non-placeholder) `SECRET_KEY`. Not a vulnerability (`.env` is gitignored), but cleaner to give production its own distinct key, so a local exposure can't affect prod. Generate with `python -c "import secrets; print(secrets.token_urlsafe(64))"`, set as Lambda's `SECRET_KEY` (users re-login once). |
| Move static assets to S3 | Web | 🟡 P2 | S | Logo, CSS, JS — serve via CloudFront for performance & HTTPS. |
| Set RDS "Publicly accessible: No" | Backend | 🟢 P3 | XS | Defense-in-depth tidy-up. RDS still has a public IP but no security group rule allows internet traffic, so it's already unreachable from outside. Removing the public IP entirely is optional cleanup. |
| Create dedicated private subnets for RDS/Lambda | Backend | 🟢 P3 | M | Currently using the default VPC's public subnets (functionally private for our purposes — Lambda has no public IP and no NAT route out). Dedicated private subnets + route tables would be the textbook setup. Do if/when the gold-standard VPC layout is wanted (also good cert learning). |
| Infrastructure Composer | AWS | 🟢 P3 | S | Visualize and document FitJournal AWS architecture using Infrastructure Composer or draw.io (AWS icons). Import existing SAM template where possible; manually add RDS, VPC, and security group components. Output to be added to README as architecture diagram for portfolio purposes. |

---

## ✅ Done

Recent shipped work, for context.

### September 2026
- **[Mobile / Web] Calendar "View All" rebuild — muscle-first, surfaces manual logs (parity)** — Rebuilt both calendars around a muscle-first "View All" model so off-routine (manual) logs finally appear. Root cause of mobile under-reporting: the mobile `WorkoutLog`/`WorkoutSession` models had a non-null `routine_day_number`, so manual logs (null day) failed to deserialize and were dropped (also undercounted the dashboard sets-per-muscle) — made both fields nullable. **Web** (`calendar.html`): the "All" view is now muscle-first (all muscles alphabetical, each exercise once) instead of day-grouped, unioning every routine exercise with any logged off-routine exercise (choice C); it's read-only (no Select column); the exercise-name column is sticky (day views also pin the Select column). Per-day filtering already surfaced manual logs by muscle and was left as-is. **Mobile** (`CalendarRepository`/`CalendarViewModel`/`CalendarScreen`): repository now fetches recent sessions across ALL days (was pre-filtering to `routine_day_number == viewingDay`, which structurally excluded manual sessions); ViewModel ports the web union/muscle/columns logic with an All vs day view mode (default current day); columns = sessions that logged any visible row (not day-number match); screen adds an "All" chip, hides Auto-select/Clear + disables tap-to-select in All. Also switched mobile cells to show **sets** (matching web) — they were showing weight, so sets-only logs (no weight) rendered as empty columns. Closes the Mobile Calendar parity item.
- **[Mobile / Web] Dashboard sticky-note restyle + sets-per-muscle module** — Restyled the mobile dashboard modules as yellow sticky notes (tilt, tape, lift shadow, black ink) via a reusable `StickyNote` composable, and added the third module missing from mobile: **Sets per Muscle** (client-side sum of `sets_completed` per muscle, This week / Last 7 days toggle), shown two-per-row column-major (most-trained top-left) with all canonical muscles seeded at 0. Matched two changes on **web**: dropped the "Training X days per week" line from the current-routine module, and made the sets-per-muscle summary show all muscle groups (0 for untrained), descending with an alphabetical tiebreaker. Mobile shows just the number (2-col, tight); web shows "N sets" (1-col) — a deliberate per-platform difference.
- **[Mobile] Single-line logging fields + unit-aware weight label** — Collapsed the two-line Weight/Sets/Reps inputs (label above field) on the WOD and manual-log screens to one line each (`Weight [__] kg  Sets [__]  Reps [__]`), shortening each exercise card. The weight unit label is now driven by the user's profile preference (kg/lb) — threaded `unitPreference` into both screens via `Navigation.kt` — instead of a hardcoded "kg".
- **[Mobile] Routine editor parity with web (curated pools, per-day types, manual days, day names, add/delete days)** — Brought the mobile routine editor up to the web model across several slices. Per_muscle days now curate an explicit exercise pool with a per-muscle draw count (a nav-destination exercise picker with cancel/commit semantics — Done keeps, back/Cancel reverts to an open-time snapshot) instead of auto-pooling every exercise at a fixed count. Added a per-day **type toggle** (By Muscle / Manual) with confirm-before-clear on switch, and full **manual day** editing (a muscle-tab picker that browses all muscles, multi-select tabs revealing stacked sections with per-tab picked-counts). Added optional **day names** (inline field, 25-char cap, shown in the editor and the two-line current-routine view alongside the day's type + muscles). Added **add/delete day** with contiguous renumber (delete confirms if the day has content, can't delete the last day) and a **locked day-count selector** on edit (create still uses it to set the count). The old `isPerMuscleOnly` view-only gate for manual/mixed routines is gone.
- **[Mobile] Real workout logging + day picker/skip + WOD persistence** — Replaced the hardcoded `sets=3, reps=10` completion stub with real per-exercise **sets/reps/weight** entry (inline fields, state keyed by `exercise_id` so it survives drag-reorder — the sets-index-by-position trap avoided), logging only the checked exercises with validation (each checked exercise needs ≥1 set). Reorganized the exercise card to a compact two-line layout (muscle + name on top with ellipsis, Weight/Sets/Reps below, drag handle + check vertically centered), retiring the weight dialog for an inline weight field. Added a **day picker/skip** on the workout Idle screen (two-line day chips, tap to set current day via the existing `set-current-day` endpoint, confirm) and made **Generate Workout** respect an existing curated selection (filtering it to the current day's exercises, matching web) or prompt before auto-generating on an empty day. WOD now **persists across in-app navigation** with an explicit **Discard/New workout** button (confirms if a workout is in progress).
- **[Mobile] Manual off-routine workout logging** — Added the mobile equivalent of web's Log Manual flow: a nav-destination screen (reached from a **Log Manually** button beside Generate Workout on the workout Idle screen) with a date field (Material3 date picker, dark-themed, capped at today, displayed `DD-MM-YYYY` / sent ISO), a familiar muscle-tab exercise picker, and a two-pane 50/50 layout (top: pick exercises; bottom: your workout with inline Weight/Sets/Reps + Log workout). Wired a `logManualWorkout` API method + `ManualLogRequest`/`ManualLogEntry` models + `WorkoutRepository.logManual` + a dedicated `ManualLogViewModel`, posting to the existing `POST /workout/log-manual/{user_id}` (writes a session + logs with `routine_day_number = NULL`, off-routine). Dates handled in UTC-midnight throughout to avoid the off-by-one. (Manual logs surfacing in the mobile Calendar depends on the Mobile Calendar parity item.)
- **[Backend] Cleaned up dead routine tables + column** — Dropped the last remnants of the pre-routine-types model. Snapshotted RDS first, then dropped the old `routine_days` (the misleadingly-named Routine header) and `routine_muscles_per_day` tables and the `exercise_is_in_routine` column on `exercises`, on **both local and RDS** — the new training-day code neither read nor wrote them, and `create_all` only adds tables so they'd lingered. Removed the matching `Routine`/`RoutineMusclePerDay` models and the column from `models.py`, plus the two `exercise_is_in_routine` fields from `schemas.py` (which would otherwise 500 the exercises endpoint once the column was gone). Done code-first (models/schemas updated and verified before dropping, so the app never ran against a mismatched schema) and as a deliberate session isolated from any feature work. The additive ALTERs that had accumulated alongside this — nullable `routine_day_number` on `workout_logs`/`workout_sessions`, and `group_index` on `training_day_exercises` — were also run on RDS as part of the same cleanup arc.
- **[Backend / Web] Manual-day exercise grouping — persisted end to end** — Manual days can group exercises into ordered supersets/blocks (A1, A2, B1…) that Log WOD renders in that order. Added `group_index` on `training_day_exercises` (with `position`); the save carries an `exercise_groups` payload, `get_routine` orders by `(group_index, position)` and returns `group_index`, the routine editor rebuilds the grouping on edit, and Log WOD renders grouped manual days in order with A1/A2 labels (ungrouped and per_muscle days unchanged). Opt-in via a “Group exercises” expander, with a “Clear grouping” escape hatch. Manual-day only. Covered by new `test_routine.py` tests. (The `group_index` ALTER is run on RDS; the dead-table cleanup is still pending — see Deployment backlog.)
- **[Web] Simplified routine setup to per-day types** — Dropped the routine-level type step (the old per_muscle / manual / mixed choice). A routine is now just a set of days, each choosing its own type (per-muscle pool or manual list) — what used to be “mixed” is now the only model. The days-per-week selector sets the count when creating (numbers `--text`, clickable) and locks on edit (`--muted`), where the count changes instead via a “+ Add day” button and a per-day “×” delete (renumbers remaining days contiguously, confirms if the day has content, won't delete the last day). Dropped the now-meaningless “Routine type” line from the current-routine view and the dashboard. Backend unchanged (day type was already per-day).
- **[Backend / Web] Log WOD — rename, day picker, and manual off-routine logging** — Renamed the Get WOD page/route to **Log WOD** (`/web/logwod`), across nav, template, and labels. Added a routine day strip to pick/skip which day to train (new `POST /workout/state/{id}/current-day` endpoint, with a confirm), and completion advances the cursor from whatever day was actually logged — so skipping leg day and doing the next one moves the cursor forward correctly (this closes the backlogged “manual day override” item). Added **manual off-routine logging**: pick a past-or-today date and log any exercises (sets/reps/weight) not tied to a routine day — a `log-manual` endpoint writes a session + logs with `routine_day_number = NULL`, does NOT advance the cursor or touch rotation counters, and feeds the dashboard sets-per-muscle summary. Required making `routine_day_number` nullable on `workout_logs` + `workout_sessions` (ALTERs run on local + RDS). Inline two-column builder (date on top; running list + Log WOD/Back on the left; a muscle-tab exercise picker that stays open on the right), unit-aware weight label. Manual logs don't appear in the Calendar yet (see the “Visualize All” backlog item).
- **[Backend] Test coverage for the new endpoints + self-healing test schema** — Added tests for the grouping round-trip (`group_index` + order preserved), manual logging (null routine day, future-date rejection, cursor NOT advanced, unowned-exercise rejection), and set-current-day (updates state, rejects out-of-range, skip-then-complete advances from the chosen day). Suite now **62 tests**, all green. Made the test schema self-healing: `conftest.py` now `drop_all`s before `create_all`, so a stale test DB (e.g. a newly-added column) can't cause a wall of unrelated failures.
- **[Web] Minor UI** — Restyled the sidebar nav (white text, larger font, white-underline hover, red-underline active; fixed an `alc()` typo that had voided the font-size rule). Added a `/favicon.ico` route that redirects to the real favicon, silencing the root-path 404 the icon set alone didn't cover.
- **[Web] Dashboard “sets per muscle” module** — New right-column dashboard module summarising sets logged per muscle group, with a **This week** (Monday-based, matching the existing quick stat) / **Last 7 days** (rolling) toggle. Sums `sets_completed` per muscle by mapping each workout log’s exercise to its muscle group; fully client-side against the existing logs + exercises endpoints (no backend change). Shows only trained muscles, sorted by volume. Supports a “~12 sets per muscle per week” tracking goal; a user-set target and showing untrained (0-set) muscles are future enhancements. Compound lifts credit only their primary muscle (see the Secondary muscle groups backlog item).
### July 2026
- **[Backend] Seeded Abs/Forearms/Biceps default exercises + full catalog seed file** — The shared `default_exercises` catalog had no Abs or Forearms entries, which blocked selecting those muscles in per_muscle routines without adding exercises by hand. Seeded a starter set (4 Abs, 2 Forearms, 1 Biceps) into **both local and RDS** via an idempotent `INSERT … WHERE NOT EXISTS` script (`Scripts/seed_abs_forearms.sql`). Also exported the full 108-exercise catalog to `Scripts/seed_default_exercises.sql`, so the default catalog is now reproducible from the repo (it had no seed file before). Only affects NEW registrations (the catalog is copied at registration); existing accounts add via the Exercises page.
- **[Backend / Web] Routine types — per_muscle, manual, mixed (full refactor)** — Replaced the single muscle-groups-per-day routine model with a first-class training-day model supporting three day types. New tables (`training_days`, `training_day_muscles`, `training_day_exercises`, `exercises_in_routine`) added additively — `create_all` builds them on cold start, no migration — while the old `routine_days`/`routine_muscles_per_day` tables and `exercise_is_in_routine` flag are left dead (cleanup backlogged). **Day types:** *per_muscle* = a selected pool of exercises per muscle with a per-session count, where generation rotates by picking the least-performed from the pool (rotation counter `times_performed` lives on `exercises_in_routine`, one shared row per exercise); *manual* = an exact exercise list taken whole; *mixed* = type chosen per day. **Reconcile-on-save:** editing a routine preserves the rotation counter for exercises that stay, deletes rows for removed ones, and enters newly-added exercises at the *minimum* counter among surviving same-muscle exercises (so a new exercise joins rotation as "next up", not force-picked or stale) — the specific behaviour requested so adding one exercise never resets the others. **Backend:** rewrote `create_or_update_routine` (whole-payload validation before any write — contiguous day numbers, per-muscle pool coverage, manual needs an exercise, ownership — then atomic clean-slate replace + reconcile), `get_routine`, `generate_next_workout` + helpers, and `complete_workout` (days-per-week now derived from training-day count; completion increments the pool counter). **Web:** rebuilt the routine page (day-count → routine-type → per-day editors; per_muscle muscle-buttons + expandable pool with counts, manual Exercises-page-style tabs + checkbox list, mixed per-day type toggle with confirm-before-clear on switch; edit mode prefills and locks routine-level type while keeping per-day toggles live for mixed; current-routine view shows one column per day, per-day type tags, and per_muscle days collapsed to muscle+count with a "see pool" expander). Migrated dashboard (3-column module grid, new routine display), getwod (new shape; manual days auto-regenerate from the routine), and calendar (new shape; subtle grey lock on manual-day select cells) to the new model. **Bug fixes along the way:** toggle endpoint 403 from a string `user_id` (int coercion); getwod false "enter sets" error from an input-index mismatch (now keyed by `exercise_id`). **Tests:** rewrote `test_routine.py` for the new contract (per_muscle/manual/mixed save+read, all validation rejections, atomic-save, string-user_id toggle), added `test_reconcile.py` (asserts actual `times_performed` — survivors unchanged, removed rows gone, added at muscle minimum), rewrote `test_workout.py` (per_muscle pool rotation, manual whole-list, completion counter), fixed `test_authorization.py`, and added an account-deletion-with-a-routine cascade test. Full suite green. Mobile still on the old shape (see Up Next).
- **[Backend / Web / Mobile] Account deletion flow** — Users can permanently delete their account and all associated data. Backend DELETE /v1/account/{user_id} with password re-authentication (cascades via ON DELETE CASCADE); public /web/delete-account confirmation page (also the Play Store Data Safety deletion URL) plus a Profile danger-zone link; mobile confirmation dialog in Profile Settings that clears the token and routes to login. Covered by test_account.py.

### June 2026
- **[Mobile] Built out the mobile Calendar screen** — replaced the placeholder DatePicker with a real per-day workout planner matching what the web Calendar does, redesigned for a narrow phone screen. Day tabs at top with dual current/selected markers (red dot + yellow fill); muscle-group section headers (uppercase, with a continuous bottom border across the frozen+scrollable seam) so a day's exercises read as grouped sections rather than one long alphabetical list; frozen exercise-name column on the left + horizontally scrollable session columns on the right (last 10 sessions for the selected day) showing weights as plain numbers (zero/missing → "—"); tap an exercise row → yellow border on the name cell, persists via `POST /v1/next-workout/toggle`; Auto-select + Clear buttons wired to the existing endpoints; right-aligned unit indicator ("Showing weights in KG"). Required adding three API surface pieces on the mobile side: `WorkoutLog` model, `LogsBySessionsRequest` + `getWorkoutLogsBySessions` service method, `ToggleSelectionRequest` + `toggleNextWorkoutSelection` service method, plus an optional `dayNumber` parameter on `clearAllSelections`. Architected the data layer as `CalendarRepository` (parallel fan-out across 5 endpoints + per-session-log fetch) + `CalendarViewModel` exposing a single `CalendarScreenState`, all following the existing `WorkoutRepository`/`WorkoutViewModel` patterns. Built in slices — data layer, table, tap-to-select, muscle grouping, unit indicator — each verified separately. Some polish items deferred (see Calendar polish backlog item).

### May 2026
- **Authorization test sweep** — added tests confirming a logged-in user can't read or modify another user's exercises, routine, workout logs, next-workout selections, workout state, or sessions, nor toggle selections for another user (the body-based inline check). All passed — confirming the earlier `verify_user_access` IDOR fix holds comprehensively across every user-scoped endpoint, with regression protection now in place.
- **Introduced API versioning (`/v1`)** — moved all JSON API routes under a `/v1` prefix using a FastAPI `APIRouter(prefix="/v1")`, so future backend changes can ship as `/v2` without breaking old installed app versions (important once the app is on the Play Store). Cross-cutting change applied in phases: backend routes + all 22 tests, then the web app (`api.js` — one-line base-URL change, since all calls share `API_URL`), then mobile (`ApiConfig.kt` — `/v1/` added to `BASE_URL`, working because all Retrofit paths are relative). The `/web/*` HTML page routes stay unversioned (server-rendered pages, not a client API contract). Done deliberately pre-launch, when there are no installed app versions to keep compatible.
- **Security hygiene pass** — ran a round of quick security checks: scoped CORS to known origins (`app.fit-journal.com` + localhost) instead of `allow_origins=["*"]` with credentials; made the DB engine's SQL echo env-driven (`DB_ECHO`, default off) so production no longer logs queries and password hashes; audited git history for a committed SECRET_KEY (found only an old placeholder — production uses a strong, distinct key, so no real exposure); and enabled Dependabot alerts + security updates for dependency vulnerability monitoring.
- **Consolidated login endpoints** — merged `/login` and `/login/mobile` (which differed only in cosmetic response fields) into a single `/login` that returns both `message` and `token_type`. Mobile now calls `/login`; the redundant `/login/mobile` was removed. A test pins the `token_type` field the mobile client depends on. Removes drift risk between two near-identical endpoints.
- Pointed mobile app at production — updated BASE_URL in ApiConfig.kt to https://app.fit-journal.com and flipped the dev flag off; verified the full critical path (login, dashboard, exercises, routine, generate, workout logging) against production with no authorization errors.
- **Fixed IDOR / added authorization checks** — endpoints took a `user_id` but only verified authentication (valid token), not ownership, so any logged-in user could read or modify another user's data by changing the ID. Added a `verify_user_access` dependency (confirms the request's `user_id` matches the authenticated user) across all 17 user-scoped endpoints, plus an inline ownership check on `/next-workout/toggle` (which takes `user_id` in the body). Surfaced by an authorization test in the new suite — a concrete example of tests catching a real vulnerability.
- **Backend test suite + CI gate** — built a pytest harness (separate MySQL test DB, per-test cleanup, auth fixture) and 21 tests covering auth, exercises, routine, profile, workout generation, page loads, and authorization. Wired a `test` job into GitHub Actions that runs against a MySQL service container before deploy; the deploy job is gated on it (`needs: test`), so failing tests block the deploy. Tests run both locally (fast feedback) and in CI (enforced gate).
- **Generate WOD — auto-generate selection + empty-state handling** — Generate WOD now guards the full flow: no routine → prompts to create one; no usable selection for the current day → persistent prompt to select in Calendar or run an automatic selection; clicking automatic selection calls the generate endpoint and re-runs generation. Manual selections are respected silently (no nagging about unselected muscle groups — deliberate intent), while auto-generated workouts show a red note for any of the day's muscle groups with no exercises in the user's library. Built on the `requireRoutine()` helper.
- **`requireRoutine()` refactor** — added a shared `requireRoutine()` helper in `api.js` (returns the routine only if `days_per_week > 0`, else null) and used it in dashboard and calendar, consolidating duplicated "does this user have a routine?" checks. Calendar gained an early empty-state guard, also fixing a pre-existing half-render bug for routine-less users.
- **No-rest-day rule** — routine save now requires every training day to have at least one muscle group, enforced both frontend (blocks save, names the empty day(s), red-highlights them) and backend (400 before the existing routine is deleted, so a rejected save doesn't wipe data).
- **Forgot-password page** — placeholder info page (reuses the auth card styling) at `/web/forgot-password`, pointing users to `info@fit-journal.com`; linked subtly under the login password field. (Real self-service reset is a P3 backlog item.)
- **Favicon** — kettlebell-on-dark-square favicon set (ico + PNG sizes + apple-touch-icon) under `static/images/favicon/`, linked in all page heads; silences the `/favicon.ico` 404 noise and adds a browser-tab icon.
- **Set up Cloudflare email** — `info@fit-journal.com` forwards to personal inbox via Cloudflare Email Routing; unblocks the forgot-password page's contact address.
- **Light/dark logo swap** — transparent-background logo (white strokes for dark mode) plus a charcoal-stroke variant for light mode, swapped via `body.light` CSS so the logo reads correctly in both themes across the header and auth pages.
- **Login screen polish** — transparent-background logo so the ruled lines show through; password show/hide toggle on login and both register fields; "Forgot password?" link added; extra left padding between the margin line and field labels.
- **Set up CI/CD with GitHub Actions** — automated build + deploy to Lambda (manual trigger), authenticated via OIDC federation (no stored AWS credentials), least-privilege IAM role scoped to the repo's main branch and single function. Resolved two deployment gotchas: added `lambda:GetFunctionConfiguration` for the update waiter, and forced `manylinux2014` wheels so compiled deps (bcrypt) load on Lambda's Amazon Linux 2 (glibc 2.26) runtime. Replaces the manual Docker→zip→upload process.
- **Fixed root routing + auth guards** — bare domain now redirects to `/web/login` (was sending everyone to `/web/dashboard`); `requireLogin()` redirects silently and gates page logic so unauthenticated visitors no longer get repeated "Please log in" alerts; login page redirects already-logged-in users straight to dashboard. Fixes the first impression for shared links.
- **Redesigned login & register pages** — notebook-style contained card with ruled lines on the `--lh` grid, red margin line, hand-drawn logo, and text resting on the lines (matching the dashboard aesthetic). Replaced the generic dark-card look.
- **Added screenshots + LICENSE to the repo** — captured app screenshots into `docs/images/` and embedded them in the README; added an MIT `LICENSE` file.
- **Decommissioned Aiven** — past its standby window; service deleted (data already migrated to RDS).
- **Network hardening** — moved Lambda into the VPC; RDS now reachable *only* via Lambda's security group (removed the `0.0.0.0/0` internet rule and the stale home-IP rule). No NAT Gateway needed since Lambda only talks to RDS. Created `fitjournal-lambda-sg`, added VPC permissions to Lambda's execution role.
- **Local MySQL dev environment** — set up local MySQL so development is location-independent (no longer dependent on whitelisting a home IP against RDS). Dedicated `fitjournal_local` user; schema built via the app; `default_exercises` seeded from RDS. Local `.env` points at `127.0.0.1`.
- **Upgraded RDS MySQL 8.0.45 → 8.4.9** — ahead of the 8.0 end-of-standard-support deadline (31 Jul 2026); migrated `fitjournal_admin` from `mysql_native_password` to `caching_sha2_password` for 8.4 compatibility; snapshot taken pre-upgrade.
- **Deployed backend to AWS Lambda + API Gateway** — FastAPI runs serverless via Mangum, live at `https://app.fit-journal.com` over HTTPS (ACM cert + API Gateway custom domain + Cloudflare DNS). Fixed deployment-specific issues along the way: bcrypt/passlib version conflict (pinned bcrypt 4.0.1), static/template relative paths, `window.location.origin` for the API URL, HTTP-API TLS policy.
- **README restructured** as an industry-standard front door; added `docs/ARCHITECTURE.md` and `docs/SCHEMA.md`.
- SECRET_KEY moved from hardcoded value to environment variable (eliminates JWT signing secret from public repo)
- Calendar "All Days" and multi-day modes — exercises grouped by day with muscle subheaders
- Migrate database from Aiven MySQL to AWS RDS MySQL (db.t4g.micro, free tier)
- Per-exercise checkoff in Get WOD (matches mobile UX, fixes BUG-001 by preventing empty workout submissions)
- Get WOD table redesign — Muscle column, narrower Exercise column, tighter Weight/Sets/Reps spacing
- Get WOD validation — sets required, reps optional, no exercises checked → error
- Get WOD UX polish — modified-weight highlight, theme-aware check circle, removed misleading placeholders, light-mode underline fix
- Add Forearms muscle group (extended `MuscleGroupEnum`, `ALTER TABLE` on three columns)
- Calendar page rebuild with notebook design system
- Calendar "Current Day" toggle replaced with red `← current day` marker; current day auto-selected on load
- Calendar — reversed date columns (newest left), Select column label
- Clean up legacy `frontend/` folder
- Light-mode color fixes for active states (saved button, selected toggle, active muscle tab, view info text)
- Jinja2 migration complete for all pages
- Dark/light mode toggle

---

## 🧭 Guiding Principles

A few standing rules for how this project evolves.

- **Schema changes need backups first.** Snapshot RDS before any `ALTER TABLE` or major version change.
- **Local dev uses local MySQL.** Start the MySQL80 service before running uvicorn; `.env` points at `127.0.0.1`. RDS is production-only now.
- **Repo is the source of truth.** Deployed code always derives from a commit — never edit code directly in the Lambda console.
- **Deploy is a separate, deliberate step.** `git push` saves to GitHub; deploying runs the GitHub Actions workflow (currently manual). Don't ship a link/feature before the page/endpoint it needs exists.
- **Compiled deps must target Lambda's runtime.** Lambda is Amazon Linux 2 (glibc 2.26); CI installs `manylinux2014` wheels so binaries like bcrypt load.
- **Page links use the `/web/` prefix.** `/web/routine`, `/web/dashboard`, etc. — bare `/routine` 404s. API endpoints (`/routine/{id}`) are separate; don't confuse the two. (Case-sensitivity bites here too — Linux/prod is case-sensitive, Windows-local isn't.)
- **Branch per feature.** Don't commit unrelated changes together.
- **Theme variables, not hardcoded colors.** Any `color: #ccc` is a future bug.
- **Migrations over recreations.** Preserve user data wherever possible.
- **Document the why, not the what.** Code shows what; commits and ROADMAP show why.
- **Every roadmap item declares its Scope.** New items go in the planning tables with a Scope value (`Mobile`, `Web`, `Backend`, `Mobile / Web`, or `All`). New Done entries use an inline `[Mobile]` / `[Web]` / `[Backend]` prefix tag. Existing pre-June-2026 Done entries left untagged.